"""
Model package for assistive cancer screening.

This package intentionally provides a model-agnostic interface and a
deterministic demo backend so the repository remains runnable without
shipping model weights.
"""
